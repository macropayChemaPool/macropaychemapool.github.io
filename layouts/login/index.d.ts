import { ILoginLayout } from '../../interfaces/layout';
/**
 * Define in your tailwind.config.ts
 *
 * If you want to use an image as background, configure the file 🔼
 *
 * Add image file into public folder
 * for example: `URL_PUBLIC` = `/images/background.('png' | 'svg' | 'jpg')`
 *
 * ```ts
 * const config: Config = {
 *  theme: {
 *    extend: {
 *      backgroundImage: {
 *        logo: url(URL_PUBLIC)
 *      }
 *    }
 *  }
 * }
 * ```
 */
export declare const LoginLayout: ({ title, logo, onClick }: ILoginLayout) => import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=index.d.ts.map